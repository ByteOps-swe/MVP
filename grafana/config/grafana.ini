[users]
  admin = admin  # Replace with a strong password
  user = user  # Replace with a strong password

[roles]
  Viewer = read  # Assign read permissions (adjust permissions as needed)

[org]
  # Organization configuration (if applicable)

[auth.generic_oauth]
  # OAuth configuration (if applicable)